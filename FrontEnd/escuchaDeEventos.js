addEventListener('keypress', () => {
    let tecla = event.key;
    let codigo = event.code;

    if (tecla == 'w') {

    } else if (tecla == 'a') {

    } else if (tecla == 's') {

    } else if (tecla == 'd')
    console.log(`Código ${codigo}`)
    console.log(`Tecla ${tecla}`)
})
